# Планировщик — прогресс сборки

## Статус: Старт

Собираем веб-страницу «планировщик» для системы задач.

- Бэкенд: Google Apps Script с JSON API (готов)
- Фронтенд: в процессе планирования

## Ожидаем

- Детали API (эндпоинты, структура данных)
- Макет страницы

## История

- 2026-06-26: Начало сборки. Детали API и макет ещё не получены.
- 2026-06-26: Получены детали API и требования к макету. Составлен план. Ждём ответы на уточняющие вопросы перед написанием кода.
- 2026-06-27: Получены ответы на все вопросы. Написан index.html (ванильный JS, без сборщиков).
- 2026-06-27: Календарь — точки заменены на цветные кольца вокруг числа (синее=дедлайн, фиолетовое=встреча, двойное=оба). Добавлена форма «＋ Добавить задачу» (action=addTask, перечитывает бэклог после добавления).
- 2026-06-27: Форма добавления — убрана кнопка-переключатель, форма всегда видна вверху страницы.
- 2026-06-27: Просроченные/сегодняшние задачи в бэклоге — красная левая полоса + красная кнопка «→ сегодня». Встречи на сегодня подсвечиваются так же.
- 2026-06-27: Добавлен SECRET_TOKEN: GET получает &token=..., POST добавляет поле token в form-data.

- 2026-06-29: Apple Touch Icon — 🐸 на светлом фоне (#f4f2ee), генерируется через Canvas в data URI; добавлены мета-теги apple-touch-icon, theme-color=#f4f2ee, apple-mobile-web-app-title=«Дела». Заголовок страницы переименован в «Дела».
- 2026-06-29: Редактирование даты прямо в бэклоге — кнопка ✎ у каждой записи; для задач открывает `<input type="date">`, для встреч — `<input type="datetime-local">`; при изменении POST action=updateDate, id, date (yyyy-MM-dd или yyyy-MM-dd HH:mm), token; после успеха перечитывает getBacklog. Бэкенд-эндпоинт updateDate будет добавлен в Apps Script.
- 2026-06-29: Баг-фикс — смена даты не сохранялась (event `change` не отправлял запрос). Добавлена явная кнопка «Сохранить» рядом с полем даты; по клику явный POST action=updateDate с form-data (action, id, date, token). Добавлено логирование console.log URL и ответа сервера. Запрос updateDate теперь виден в Network.

- 2026-06-29: «Сегодня» теперь хранится на сервере. Кнопки «→ сегодня» и ↩ шлют POST `setSession` (session='Сегодня' / ''), блок «Сегодня» на загрузке тянется из `getToday` (через `fetchBacklog`, Promise.all с getBacklog). Убрана `addToTodayDirect` (память) → `sessionToToday` (сервер). Выбор «сегодня» переживает F5.

- 2026-06-29: Блок «Банка наград» — кошелёк `balance/50` + полоска прогресса, кнопки «Удиви меня 🎲» (mode=random) и «Выбрать награду» (раскрывает rewards, mode=choose). Кнопки disabled при balance<50 с подписью «нужно ещё N». GET `getBank` на загрузке/после разблокировки, POST `unlockReward` через apiPost; источник правды — getBank. Баннер «Тебе выпало: …».

- 2026-06-29: Банка — список наград стал витриной: «Выбрать награду» раскрывает rewards всегда (и при balance<50, просто просмотр «на что копить»). Кнопка «взять» у награды показывается только при canUnlock=true; при недостатке баллов — лишь название, без disabled-кнопок. «Удиви меня 🎲» по-прежнему disabled при balance<50.

- 2026-06-29: Блок «Привычки» — на каждую привычку сетка квадратов на дни месяца (число дней из month, не хардкод), счётчик countThisMonth/дней, подпись +баллы. Закрашены дни из daysThisMonth, «сегодня» выделен рамкой и кликабелен → POST toggleHabit → перечитка getHabits (+ fetchBank, привычки влияют на баланс). Прошлые/будущие дни некликабельны. GET getHabits на загрузке/F5. Квадраты крупные (min 26px) под палец.

- 2026-06-30: Привычки — вертикальная раскладка (каждая привычка = столбец: заголовок сверху, дни 1..N сверху вниз, столбцы рядом со скроллом вбок), блок перенесён под календарь в левую колонку (.left-col оборачивает календарь+привычки). Листание синхронизировано с календарём: стрелки ‹›/toggle зовут fetchHabits(shownMonthStr()), getHabits принимает &month=YYYY-MM. Клик только по «сегодня» (есть лишь в реальном текущем месяце), прошлые месяцы — только просмотр.

- 2026-06-30: Мобильная раскладка (@media max-width 660px) — `.main-grid` стал flex-колонкой с order: бэклог (`.backlog-panel`) выше, `.left-col` (календарь+привычки) ниже; десктоп (две колонки grid) не тронут. Заголовки привычек `.habit-col-head` сделаны липкими (sticky top:0, фон var(--surface)), `.habit-cols` получил overflow-y: visible.

- 2026-06-30: Переименованы подписи квадрантов по матрице Эйзенхауэра в двух местах: `<select id="f-quadrant">` и объект `Q_LABELS` — теперь A · важно и срочно, B · важно, не срочно, C · срочно, не важно, D · не срочно, не важно. Значения (value="A".."D") и ключ «Встреча» не тронуты.

- 2026-06-30: Бейдж «день закрыт» в блоке «Сегодня» — если `getToday` возвращает `dayClosedBonus > 0`, рядом с предупреждениями рендерится зелёный `.day-closed-badge` вида «✅ день закрыт +N». Поле читается в `fetchBacklog` вместе с остальными полями `tRes`, хранится в `state.dayClosedBonus`, применяется в `renderToday()`.

- 2026-06-30: Интерфейс разбит на две вкладки «Задачи» и «Привычки» (SPA, без перезагрузки). Сегментированный переключатель `.tabs-bar` вверху страницы. Вкладка «Задачи» содержит форму добавления, «Сегодня», банку наград и двухколоночный main-grid с полным календарём. Вкладка «Привычки» содержит компактную листалку месяца (‹ Июнь 2026 ›) и блок привычек. Переключение: JS-переменная `activeTab`, класс `.hidden` (`display:none`) на неактивной панели. Общий месяц: вынесена функция `changeMonth(delta)`, которую зовут обе пары стрелок (cal-prev/cal-next и mn-prev/mn-next); `state.year/month` — единственный источник правды; смена месяца с любой вкладки обновляет и полный календарь, и компактный заголовок (`renderMonthNav()`). localStorage не используется.

- 2026-06-30: Раскладка привычек изменена на календарные блоки. Каждая привычка — карточка (`background: var(--bg)`, border, border-radius: 8px) с заголовком и сеткой дней `display: grid; grid-template-columns: repeat(7, 1fr); gap: 3px` — день 1 всегда первым, без выравнивания по дням недели. Карточки расположены по `repeat(auto-fit, minmax(340px, 1fr))` — два блока в ряд на широком экране, один на узком. Горизонтальный скролл `.habit-cols` убран. Ячейки `aspect-ratio: 1` (квадратные, размер от грида). `position: sticky` с заголовка убран. Логика toggleHabit, классы filled/is-today, смена месяца — не тронуты.

- 2026-06-30: Календарь — время встреч и короткие названия дел прямо в ячейках. `renderCalendar()` строит `dayItems` (meetings с полем `time = task.date.slice(11,16)`, tasks). В каждой ячейке: под числом `.cal-meeting-time` (фиолетовый, 9px) — время ранних встреч через запятую (desktop + mobile); `.cal-cell-items` — до 3 строк `.cal-item` (встречи «14:30 Врач», задачи по имени) + `.cal-item-more` «+N» при переполнении, overflow:hidden/ellipsis/nowrap. На мобильном (@media max-width: 660px): `.left-col` order:-1 → календарь идёт первым на вкладке «Задачи»; `.cal-meeting-time` скрыт (дублируется в items); ячейки min-height:62px; `.cal-cell-items` display:block. Кольца-индикаторы, клик по дню (renderDayPanel), выбор/подсветка is-today/is-selected, листание месяцев — не тронуты. Названия экранированы через esc().

- 2026-06-30: Время встреч теперь видно в строке задачи и в панели дня. Добавлена хелпер-функция `meetingTime(task)` (рядом с `taskDate`) — возвращает `HH:mm` из `task.date.slice(11,16)`, только если `task.quadrant === 'Встреча'` и в дате есть время (длина > 10), иначе пустую строку. `renderTaskRow`: в meta вместо среза даты до 10 символов — для встреч выводится `YYYY-MM-DD HH:mm`, для обычных задач — как раньше только дата. `renderDayPanel`: перед названием встречи выводится время («18:00 Маник»), для обычных задач — только название. Форма редактирования даты, сортировка, вкладки, календарь не тронуты.

- 2026-06-30: Полноразмерный календарь вынесен на отдельную третью вкладку «Календарь» (панель вкладок теперь «Задачи» / «Календарь» / «Привычки», `switchTab` расширен на `pane-calendar`/`tab-calendar`). Общая сборка дел по дням выделена в `collectDayItems()` (бывшее тело `renderCalendar()`, без дублирования логики), `renderCalendar()` теперь только считает `deadlineDates/meetingDates/dayItems` и обновляет оба заголовка месяца, а рисование делегирует двум функциям: `renderCompactGrid()` — маленький календарь на «Задачах» (id `cal-grid`), упрощён до одних колец-индикаторов (`calNumClass`), без времени и текста дел; клик по дню по-прежнему выбирает `state.selectedDay` и открывает `renderDayPanel()`. `renderFullGrid()` — большой календарь на «Календаре» (id `cal-grid-full`, новые классы `.cal-cell-full/.cal-num-full/.cal-item-full/.cal-item-more-full`), под числом видно без клика до 3 пунктов («ЧЧ:ММ Название» для встреч, только название для задач) + «+N», обрезка через overflow:hidden/ellipsis/nowrap, названия экранированы esc(); клик по дню ничего не делает (только просмотр). Месяц общий: `changeMonth()`/`state.year`/`state.month` не менялись, стрелки `cal-full-prev/next` навешаны на тот же `changeMonth`, поэтому большой и маленький календари и привычки листаются синхронно. Удалены мёртвые CSS-классы `.cal-meeting-time/.cal-cell-items/.cal-item/.cal-item-more` и их мобильные оверрайды (текст в компактных ячейках больше не рендерится).

- 2026-06-30: Большой календарь (вкладка «Календарь») — цвета и поворот на мобильном. Названия дел в ячейках раскрашены: встречи — фиолетовым (`var(--q-v)`, класс `.cal-item-full--meeting`), дедлайны/задачи — синим (`var(--accent)`, класс `.cal-item-full--task`) — те же переменные, что у колец `.cal-ring-meeting`/`.cal-ring-task`; `renderFullGrid()` теперь строит список пунктов как `{kind, label}` и подставляет модификатор класса вместо плоских строк. Фон ячеек не менялся. На мобильном (`@media max-width: 660px`) сетка большого календаря (`.cal-grid-full` вместе с `.cal-dow`-подписями, БЕЗ заголовка месяца/стрелок — они специально оставлены неповёрнутыми и читаются в обычной портретной ориентации, чтобы месяц можно было листать не наклоняя телефон) обёрнута в `.cal-full-rotate-wrap` > `.cal-full-rotate-inner` и повёрнута через `transform: rotate(-90deg)` (против часовой стрелки на CSS-уровне ⇒ чтобы прочитать календарь, нужно физически повернуть телефон ПО часовой стрелке на 90°, верхний край — вправо). `.cal-full-rotate-wrap` резервирует в потоке страницы уже повёрнутый (визуальный) прямоугольник — `width: 92vw; height: 78vh` — а `.cal-full-rotate-inner` заранее задан с обменянными местами шириной/высотой (`width: 78vh; height: 92vw`), так что после поворота на 90° он визуально совпадает с wrap и не наезжает на соседние блоки; `transform-origin: center center`, центрирование — `position: absolute; top/left: 50%; translate(-50%,-50%)`. Внутри повёрнутого блока ячейки (`.cal-cell-full`) и текст (`.cal-num-full/.cal-item-full/.cal-item-more-full`) уменьшены (padding/font-size/`grid-auto-rows: 1fr`), чтобы все 6-7 строк месяца уместились по короткой стороне экрана без обрезки. На десктопе (>660px) `.cal-full-rotate-wrap/-inner` — обычные `width:100%` блоки без трансформации, вид не меняется. Маленький календарь на «Задачах», вкладки, синхронная смена месяца, клик по дню, кольца — не тронуты.

- 2026-06-30: Направление поворота большого календаря на мобильном изменено на против часовой стрелки (`rotate(90deg)` вместо `rotate(-90deg)`), центровка/габариты не менялись.

- 2026-06-30: Три мелких правки большого календаря (вкладка «Календарь»). (1) Подписи дней недели придвинуты к сетке чисел на мобильном — подписи (`.cal-dow`) лежат в той же CSS-grid, что и числа, поэтому расстояние до сетки — это их `padding-bottom`, а не общий `gap` грида (gap трогать нельзя, иначе сдвинутся и сами числа); добавлено `.cal-full-rotate-inner .cal-dow { padding: 3px 0 1px }` только внутри мобильного `@media`, десктоп не тронут. (2) Клик/тап по дню на большом календаре теперь открывает полный список дел дня — `renderFullGrid()` добавляет `data-date` на непустые `.cal-cell-full` и вешает `click` → `state.selectedDay = ds; renderDayPanel()` (раньше клик ничего не делал); `renderDayPanel()` расширена, чтобы писать один и тот же HTML сразу в оба контейнера — `#day-panel` (маленький календарь) и новый `#day-panel-full` (добавлен в разметку `pane-calendar`, вне повёрнутого `.cal-full-rotate-wrap`, чтобы текст читался в обычной ориентации, а не повёрнутым). Добавлены `cursor:pointer`/`:hover` на `.cal-cell-full` (кроме `.empty`). (3) В ячейках календаря названия обрезаются до первых ~3 слов с многоточием — новая функция `truncateWords(str, n=3)` (обрезка по словам через `split(/\s+/)`, не по символам), применена и к встречам (после времени: «18:00 Маник…»), и к задачам; полное название по-прежнему доступно через панель дня (клик, см. пункт 2). Экранирование `esc()` не менялось. Поворот, раскладка, соответствие дней недели, маленький календарь, вкладки, синхронная смена месяца и цвета встреч/дедлайнов — не тронуты.

- 2026-06-30: Две правки повёрнутого большого календаря на мобильном. (1) Панель подробностей дня (`renderDayPanel`, вывод в `#day-panel-full`) теперь повёрнута тем же `rotate(90deg)`, что и сетка — своя пара `.day-panel-rotate-wrap`/`.day-panel-rotate-inner` (та же схема, что у `.cal-full-rotate-wrap`/`-inner`: wrap резервирует в потоке уже повёрнутый прямоугольник `92vw × 22vh`, inner заранее задан с обменянными width/height `22vh × 92vw` и `transform: translate(-50%,-50%) rotate(90deg)`). Разметка стоит в HTML сразу после `.cal-full-rotate-wrap`, поэтому в повёрнутой системе координат панель идёт следом за сеткой (не поверх/сбоку); `overflow-y: auto` на inner — страховка, если дел в дне много и они не помещаются в отведённую полосу. На десктопе `.day-panel-rotate-wrap/-inner` — обычные `width:100%` блоки без трансформации (как и у `.cal-full-rotate-wrap/-inner`), вид не меняется. Проверено скриншотами (playwright, мок API): панель не наезжает на сетку, полностью видна, при обратном повороте («физическая» проверка) весь текст читается прямо. (2) Подписи дней недели придвинуты вплотную к сетке — реальная причина большого отступа была не в `padding-bottom` (это правили раньше, не помогало), а в `grid-auto-rows: 1fr` на `.cal-full-rotate-inner .cal-grid-full`: он растягивал строку подписей на ту же долю высоты, что и строку недели, из-за чего после поворота между подписями и числами появлялся широкий пустой столбец. Добавлено `grid-template-rows: auto` (задаёт только первую строку — по размеру содержимого), `grid-auto-rows: 1fr` по-прежнему делит место между строками недель поровну. Порядок и соответствие подписей строкам (ПН…ВС напротив своих недель) не менялись. Поворот, раскладка чисел, десктопный вид, маленький календарь, вкладки, клик по дню, синхронная смена месяца — не тронуты.

- 2026-06-30: Редактирование и удаление задач в бэклоге (фронт; бэкенд `editTask`/`deleteTask` уже готов). `renderTaskRow()` — по образцу кнопки-карандаша даты (`btn-pencil`/`editingDateId`) добавлены кнопки `✏️` (`.btn-task-edit`) и `🗑` (`.btn-task-delete`) с `data-id`, новое состояние `state.editingTaskId`. По клику на `✏️` под строкой раскрывается `renderTaskEditForm(task)` — переиспользует контейнер `.subtask-form` (та же визуальная рамка/паддинги, что у формы шагов) и содержит: `input.task-edit-name` (название, предзаполнено), `select.task-edit-quadrant` (A/B/C/D/Встреча — те же подписи, что в `#f-quadrant` формы добавления, вынесены в константу `QUADRANT_OPTIONS`, текущее значение выбрано `selected`), `select.task-edit-size` (S/M/L, текущее — `selected`), `textarea.task-edit-steps` (шаги по одному на строку, как в `renderSubtaskForm`) и подпись-предупреждение о шагах. Открытие формы редактирования закрывает форму даты/шагов и наоборот (не могут быть открыты одновременно). По «Сохранить» — `handleSaveTaskEdit()`: берёт значения из полей внутри `btn.closest('.task-edit-form')`, для шагов сравнивает новые строки с исходными (`parseSubtasks`) и сохраняет отметку `:1`, если текст шага не изменился (иначе `:0` — новый/изменённый шаг всегда стартует невыполненным, о чём предупреждает подпись в форме), затем `apiPost({action:'editTask', id, task, quadrant, size, subtasks})` → `fetchBacklog()`. Пустое название блокирует сохранение (подсветка поля). По «Отмена» форма закрывается без запроса. Кнопка `🗑` → `handleDeleteTask(id)`: `confirm('Удалить задачу?')`, только при подтверждении `apiPost({action:'deleteTask', id})` → `fetchBacklog()` (удаление необратимо, поэтому обязательное подтверждение). Стили кнопок разделяют правила с `.btn-pencil` (тот же приглушённый вид), у корзины — красный hover. Дата ✎, «→ сегодня», «готово», реопен, сортировка, вкладки, календарь — не тронуты; никаких новых запросов к серверу, кроме `editTask`/`deleteTask`; localStorage не используется.

- 2026-06-30: Два карандаша у задачи объединены в один (фронт; бэкенд `editTask` уже принимает и сохраняет поле `date`). Убраны: кнопка `.btn-pencil` (✎, «Изменить дату»), `state.editingDateId`, функция `renderDateEditForm()` и её обработчики (`.date-edit-save`/`.date-edit-cancel`) — старая логика формата даты не выброшена, а вынесена в `taskEditDateField(quadrant, date)` (встреча → `datetime-local`, значение `date.slice(0,16).replace(' ','T')`; иначе → `date`, значение `date.slice(0,10)`; без даты — пусто) и используется внутри `renderTaskEditForm()`. Поле `input.task-edit-date` теперь стоит в одной строке с селектами важности/размера (`.task-edit-row`, которому добавлен `flex-wrap: wrap` на случай тесноты). Тип поля подстраивается под важность динамически: новый обработчик `change` на `.task-edit-quadrant` (в `renderBacklog()`) находит `.task-edit-date` в той же форме, переключает `type` между `date`/`datetime-local` и конвертирует значение (обрезает до `YYYY-MM-DD` или дополняет `T00:00`) — не только при открытии формы, но и на лету при смене важности. Сохранение осталось ОДНИМ запросом: `handleSaveTaskEdit()` берёт значение `.task-edit-date`, приводит к формату бэка (для встречи — `replace('T',' ')`, иначе как есть; пусто — `date: ''`, бэк очищает дату) и отправляет вместе с остальными полями в `apiPost({action:'editTask', id, task, quadrant, size, subtasks, date})`. `handleUpdateDate()` (отправлял отдельный `updateDate`) удалён как неиспользуемый; `handleDeleteTask()` избавлен от ссылки на `editingDateId`. Осталась одна кнопка `✏️` (`.btn-task-edit`) + `🗑` (`.btn-task-delete`) + «→ сегодня» в `.task-actions`. Проверено плейрайтом (мок API): для обычной задачи поле `date`, при переключении важности на «Встреча» — `datetime-local` со значением `…T00:00`, обратно — снова `date`; сохранение отправляет корректно сконвертированную дату (включая очистку пустым значением); карандаш даты нигде на странице не остался. Удаление, «→ сегодня», «готово», реопен, сортировка, вкладки, календарь, форма шагов при вытаскивании в «Сегодня» — не тронуты; экранирование `esc()` сохранено; localStorage не используется.

- 2026-06-30: Привычки теперь можно отмечать за прошлые дни (фронт; бэкенд `toggleHabit` уже принимает `day`, без него берёт сегодня). В `renderHabits()` для каждой ячейки, помимо `filled`/`isToday`, считается `clickable = ds <= today` (сравнение строк `'YYYY-MM-DD'` работает как сравнение дат) — это одно условие само по себе покрывает все три случая: в прошлых показанных месяцах кликабельны все дни, в текущем месяце — по сегодняшний день включительно, в будущих месяцах — ни один. Кликабельным ячейкам (класс `.habit-cell.clickable`, добавляется вместе с `is-today`, если день — сегодня) проставляются `data-habit` и `data-day="${ds}"`; раньше data-атрибуты и обработчик клика были только у `.is-today`. Обработчик клика перевешан с `.habit-cell.is-today` на `.habit-cell.clickable` и передаёт день дальше. `handleToggleHabit(name, day)` получил второй параметр и шлёт `apiPost({action:'toggleHabit', habit:name, day})` (день передаётся, только если есть — сохранена обратная совместимость на случай вызова без него), затем как и раньше `fetchHabits(shownMonthStr())` + `fetchBank()`. CSS: `cursor:pointer`/`:active{scale(0.92)}` вынесены из `.habit-cell.is-today` в отдельный `.habit-cell.clickable`, у `.is-today` остались только рамка/жирность (граница сегодняшнего дня по-прежнему видна отдельно от «кликабельности»); `.filled` (заливка/цвет отметки) не менялся — прошлые отмеченные дни выглядят так же, как раньше выглядел только сегодняшний. Проверено плейрайтом (мок API, `today: '2026-07-11'`): в текущем месяце кликабельны ровно дни 1–11, день 20 — без класса `clickable` и без реакции на клик; в прошлом показанном месяце кликабельны все 30 дней, в будущем — ни один; клик по прошлому дню и по сегодня шлют `day` с верной датой. Начисление баллов, sticky/раскладка карточек привычек, листание месяцев и синхронизация с календарём, вкладки — не тронуты; localStorage не используется.

- 2026-06-30: Визуальная тема сменена на мягкий «лягушачий» пастельно-зелёный стиль (только `index.html`, `<style>`; разметка и JS не тронуты). `:root`: `--bg` #e6f0da, `--surface` #f8fbf2, `--border` #cfe0bd, `--text` #2e3a2a, `--muted` #7a8a6e, `--accent` синий→зелёный #6ba368, `--r` 10px→16px. Квадранты смягчены (`--q-a`/`--danger` #c0503f, `--q-b` #c98a3e, `--q-c` #2e8b57, `--q-d` #8a9a80, `--q-v` #8e5aa8 остался фиолетовым). Добавлена новая переменная `--deadline` (#2f7d7d, бирюзовый) — раньше кольцо/текст дедлайна в календаре (`.cal-ring-task`, `.cal-ring-both`, `.cal-item-full--task`) сидели на `--accent`; после перекраски `--accent` в зелёный они бы слились с фоном, поэтому дедлайны переведены на отдельный, хорошо читаемый на зелёном цвет, отличный и от встреч (фиолетовый), и от акцента. Все хардкод-цвета, выбивавшиеся из палитры (`#eee`, `#f0f0f0`, `#eef4fb`, `#fde8e6`, `#e8f5ed`, `#f9f8f5`, `#2a9d5c`, `#1a6e3c`, `#333`, `#aaa` и т.д.), подобраны в тон новой зелёной гамме. Карточкам на `var(--r)` (`.today-block`, `.panel`, `.bank-block`, `.habits-block`, `.add-form-panel`, `.month-nav`) и `.today-card` добавлены мягкие размытые тени (`box-shadow` с низкой прозрачностью, большим blur, зеленоватым оттенком вместо чёрного). Мелкие радиусы кнопок/полей/тегов увеличены на 2px для более округлого вида. Мета-тег `theme-color` и `fillStyle` канвы apple-touch-icon (фон под 🐸) обновлены с #f4f2ee на #e6f0da, чтобы совпадать с новым фоном. Проверено: количество `{`/`}` в `<style>` совпадает (178/178), диф затрагивает только цвета/тени/радиусы — ни разметка, ни JS-логика не менялись, localStorage не используется.

## TODO
- Бонус +30 баллов за все закрытые задачи дня — фронт готов (читает dayClosedBonus из getToday); бэкенд ещё не реализован
